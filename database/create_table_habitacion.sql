CREATE TABLE habitaciones (
    num_habitacion  VARCHAR PRIMARY KEY
                            NOT NULL,
    hab_capacidad   VARCHAR NOT NULL,
    hab_acomodacion VARCHAR NOT NULL,
    hab_bano        VARCHAR NOT NULL
);
